<hibernate-configuration>
    <session-factory>

        <!-- DATABASE CONNECTION -->
        <property name="hibernate.connection.driver_class">
            com.mysql.cj.jdbc.Driver
        </property>

        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/employee_management_system_mapping
        </property>

        <property name="hibernate.connection.username">
            YOUR_DB_USERNAME
        </property>

        <property name="hibernate.connection.password">
            YOUR_DB_PASSWORD
        </property>

        <!-- HIBERNATE SETTINGS -->
        <property name="hibernate.dialect">
            org.hibernate.dialect.MySQL8Dialect
        </property>

        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
        <property name="hibernate.hbm2ddl.auto">update</property>

        <property name="hibernate.current_session_context_class">
            thread
        </property>

        <!-- ENTITY MAPPING -->
        <mapping class="com.employee.entity.Employee"/>
        <mapping class="com.employee.entity.Department"/>
        <mapping class="com.employee.entity.EmployeeProfile"/>
        <mapping class="com.employee.entity.Project"/>

    </session-factory>
</hibernate-configuration>